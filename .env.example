# CDL Gateway Configuration

# Application Settings
APP_NAME="CDL Gateway"
ENVIRONMENT=production
LOG_LEVEL=INFO

# AWS Credentials (provided by EC2 Instance Profile / IAM Role in production)
# AWS_ACCESS_KEY_ID=your-aws-access-key-id  # Only needed for local development
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key  # Only needed for local development

# === AWS Secrets Manager에서 자동 로드되는 환경변수들 ===
# 프로덕션에서는 아래 값들이 AWS Secrets Manager에서 자동으로 로드됩니다.
# 로컬 개발 시에만 주석을 해제하고 실제 값을 입력하세요.

# API Keys (loaded from AWS Secrets Manager)
# OPENAI_API_KEY=your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key  
# GOOGLE_API_KEY=your-google-api-key
# OPENAI_API_KEY_REALTIME=your-openai-realtime-key
# ANTHROPIC_API_KEY_REALTIME=your-anthropic-realtime-key
# GOOGLE_API_KEY_REALTIME=your-google-realtime-key

# AWS Configuration (loaded from AWS Secrets Manager)
# AWS_REGION=ap-northeast-2

# RabbitMQ 설정 (loaded from AWS Secrets Manager)  
# RABBITMQ_USER=your-rabbitmq-user
# RABBITMQ_PASSWORD=your-rabbitmq-password
# RABBITMQ_HOSTNAME=your-rabbitmq-hostname
# RABBITMQ_PORT=5672
# RABBITMQ_PORT2=5673
# RABBITMQ_PORT3=5674

# 클러스터 연결 설정
RABBITMQ_CONNECTION_TIMEOUT=15
RABBITMQ_RETRY_ATTEMPTS=5
RABBITMQ_RETRY_DELAY=3.0
RABBITMQ_HEARTBEAT=300

# AWS MQ Settings (fallback - 필요시에만 설정)
# RABBITMQ_BROKER_ID=your-broker-id
# RABBITMQ_REGION=ap-northeast-2

# Server Settings
GUNICORN_WORKERS=3
UVICORN_LOG_LEVEL=info